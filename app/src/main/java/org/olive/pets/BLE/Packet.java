package org.olive.pets.BLE;

/**
 * Created by KMJ on 2017-05-24.
 */

public class Packet {
/*
    enum Oper {
        OP_Discon = -1,
        OP_Report = 1,
        OP_Train,   // 2
        OP_Sync,    // 3
    };

    const int Postures = 8;

    enum Pos
    {
        P_Unknown = 0,
        P_Lie,     // 1
        P_LieSide, // 2
        P_LieBack, // 3
        P_Sit,     // 4
        P_Stand,   // 5
        P_Walk,    // 6
        P_Run      // 7
    };

    enum Attr
    {
        A_Mean = 1,
        A_Stdev,    // 2
        A_Time      // 3
    };

    struct Packet
    {
        int prefix;
        int param;
        int value;
    };
    struct Packet pack;

    int param(int pos, int attr)
    {
        return (pos << 4) + attr;
    }

    int pos(int param)
    {
        return param >> 4;
    }

    int attr(int param)
    {
        return param & 0x0F;
    }

*/
}
