<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.olive.pets">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- android:hardwareAccelerated="true" while adding -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/dog"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BTActivity" />
        <activity android:name=".DailyReportActivity" />
        <activity android:name=".DailyWorkRateActivity"/>
        <activity android:name=".DailyActivityActivity"/>
        <activity android:name=".ManagerInfoActivity"/>
        <activity android:name=".DogInfoActivity"/>
        <activity android:name=".chart.PieChartActivity"/>
    </application>
</manifest>